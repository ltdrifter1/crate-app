{
  "name": "planetmp3-functions",
  "description": "Planet MP3 Stripe billing + Firebase membership sync",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "private": true,
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "node --check index.js && node --check lib/plans.js && node --check lib/syncMembership.js && node --check lib/listening.js && node --check lib/homeLite.js && node --check lib/catalogJson.js && node --check lib/stationChat.js",
    "test": "node --test lib/plans.test.js lib/listening.test.js lib/syncMembership.test.js lib/homeLite.test.js lib/catalogJson.test.js lib/stationChat.test.js"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0",
    "stripe": "^22.4.0"
  }
}
