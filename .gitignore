node_modules/
.firebase/
serviceAccountKey.json
src.zip
build.zip
audio/
covers/
titles-review.csv
genres-review.csv
*.csv
.env
.env.*
!.env.example
# NOTE: `build/` is tracked for Cloudflare Pages while the dashboard
# has an empty build command (output dir = build). Once you set
# Build command to `npm run build`, add `build/` here again.
